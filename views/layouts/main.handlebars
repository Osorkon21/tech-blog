<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link href="/css/style.css" rel="stylesheet" />
  </head>

  <body class="mb-5">
    <header>
      <div class="top-bar p-4 mb-3">
        <div class="nav-links">
          <a class="text-decoration-none me-5 link-light h5" href="/">Home</a>

          {{#if loggedIn}}
            <a class="text-decoration-none me-5 link-light h5" href="/dashboard">Dashboard</a>
            <button id="logout" class="btn btn-danger">Logout</button>
          {{else}}
            <a class="text-decoration-none link-light h5" href="/login">Login</a>
          {{/if}}
        </div>

        <div class="decorative-title border rounded-3 p-1 h4 fw-bold d-inline-flex mt-4 px-3">
          {{#if dashboard}}
            Your Dashboard
          {{else}}
            The Tech Blog
          {{/if}}
        </div>
      </div>
    </header>

    {{{body}}}

    {{#if loggedIn}}
      <script src="/js/logout.js"></script>
    {{/if}}
  </body>

</html>
